<div class="resolution-content-simplified">
  <% refund = order.refund %>
  
  <!-- Simplified Resolution Header -->
  <div class="resolution-header-clean">
    <div class="resolution-alert">
      <i class="fas fa-exclamation-triangle"></i>
      <div class="alert-content">
        <h6>Resolution Required</h6>
        <p><%= refund.refund_reason.humanize %> • $<%= number_with_precision(refund.refund_amount, precision: 2) %></p>
      </div>
    </div>
  </div>

  <!-- Go to Resolution Center Button -->
  <div class="resolution-action">
    <%= link_to resolution_path(order_id: order.id), 
                class: "resolution-center-btn",
                data: { 
                  turbo_frame: "main_content",
                  turbo_action: "replace" 
                } do %>
      <i class="fas fa-tools"></i>
      <span>Go to Resolution Center</span>
      <i class="fas fa-arrow-right"></i>
    <% end %>
  </div>

  <!-- Recent Activity Feed (3-4 items) -->
  <div class="activity-feed-mini">
    <div class="activity-header">
      <i class="fas fa-history"></i>
      <span>Recent Activity</span>
    </div>
    <div class="activity-items">
      <% order.unified_timeline(4).each do |activity| %>
        <div class="activity-item">
          <div class="activity-icon activity-<%= activity[:type] %>">
            <% case activity[:type] %>
            <% when 'refund' %>
              <i class="fas fa-money-bill-wave"></i>
            <% when 'dispatch' %>
              <i class="fas fa-truck"></i>
            <% else %>
              <i class="fas fa-circle"></i>
            <% end %>
          </div>
          <div class="activity-content">
            <span class="activity-action"><%= activity[:action].humanize %></span>
            <small class="activity-time"><%= time_ago_in_words(activity[:timestamp]) %> ago</small>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Order Has Shipped - AutoXpress</title>
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
      line-height: 1.7;
      color: #1f2937;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
      font-size: 16px;
    }
    .email-wrapper {
      max-width: 640px;
      margin: 40px auto;
      background: #ffffff;
      border-radius: 20px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    
    /* Modern Header */
    .header {
      background: linear-gradient(135deg, #1f2937 0%, #374151 100%);
      padding: 40px 32px;
      text-align: center;
      position: relative;
    }
    .header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #dc2626 0%, #ef4444 50%, #f97316 100%);
    }
    .logo {
      font-size: 32px;
      font-weight: 800;
      color: white;
      margin: 0 0 8px 0;
      letter-spacing: -0.025em;
    }
    .header-subtitle {
      color: #d1d5db;
      font-size: 18px;
      font-weight: 500;
      margin: 0;
    }
    
    /* Content Area */
    .content {
      padding: 48px 32px;
    }
    .greeting {
      font-size: 20px;
      font-weight: 600;
      color: #111827;
      margin: 0 0 16px 0;
      line-height: 1.4;
    }
    .intro-text {
      font-size: 17px;
      color: #4b5563;
      margin: 0 0 40px 0;
      line-height: 1.6;
    }
    
    /* Tracking Hero Section */
    .tracking-hero {
      background: linear-gradient(135deg, #fef2f2 0%, #fef7f7 100%);
      border: 2px solid #fee2e2;
      border-radius: 16px;
      padding: 32px;
      text-align: center;
      margin: 40px 0;
    }
    .tracking-hero h3 {
      font-size: 20px;
      font-weight: 700;
      color: #dc2626;
      margin: 0 0 16px 0;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
    .tracking-number {
      background: #ffffff;
      border: 2px solid #dc2626;
      border-radius: 12px;
      padding: 16px 24px;
      font-family: 'SF Mono', Monaco, 'Cascadia Code', monospace;
      font-size: 20px;
      font-weight: 700;
      color: #dc2626;
      letter-spacing: 2px;
      margin: 20px 0;
    }
    .track-button {
      display: inline-block;
      background: #dc2626;
      color: #ffffff;
      padding: 16px 32px;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: 16px;
      margin: 20px 0 0 0;
      transition: all 0.2s ease;
    }
    .track-button:hover {
      background: #b91c1c;
      transform: translateY(-1px);
    }
    
    /* Package Preparation */
    .package-prep {
      background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%);
      border: 2px solid #fde68a;
      border-radius: 16px;
      padding: 32px;
      text-align: center;
      margin: 40px 0;
    }
    .package-prep h3 {
      font-size: 20px;
      font-weight: 700;
      color: #d97706;
      margin: 0 0 16px 0;
    }
    .package-prep p {
      font-size: 16px;
      color: #92400e;
      margin: 12px 0;
      line-height: 1.6;
    }
    
    /* Details Grid */
    .details-grid {
      background: #f9fafb;
      border-radius: 16px;
      padding: 32px;
      margin: 40px 0;
    }
    .details-grid h4 {
      font-size: 18px;
      font-weight: 700;
      color: #111827;
      margin: 0 0 24px 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .detail-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 16px 0;
      border-bottom: 1px solid #e5e7eb;
    }
    .detail-item:last-child {
      border-bottom: none;
    }
    .detail-label {
      font-size: 16px;
      color: #6b7280;
      font-weight: 500;
      margin-right: 8px;
    }
    .detail-value {
      font-size: 16px;
      color: #111827;
      font-weight: 600;
      text-align: right;
    }
    .status-badge {
      background: #dc2626;
      color: white;
      padding: 6px 12px;
      border-radius: 6px;
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    /* What to Expect */
    .expectations {
      margin: 40px 0;
    }
    .expectations h4 {
      font-size: 18px;
      font-weight: 700;
      color: #111827;
      margin: 0 0 20px 0;
    }
    .expectations ul {
      margin: 0;
      padding: 0;
      list-style: none;
    }
    .expectations li {
      font-size: 16px;
      color: #4b5563;
      margin: 12px 0;
      padding-left: 24px;
      position: relative;
    }
    .expectations li::before {
      content: '✓';
      position: absolute;
      left: 0;
      color: #10b981;
      font-weight: bold;
    }
    
    /* Footer */
    .footer {
      background: #f8fafc;
      padding: 32px;
      text-align: center;
      border-top: 1px solid #e5e7eb;
    }
    .footer-brand {
      font-size: 18px;
      font-weight: 700;
      color: #374151;
      margin: 0 0 8px 0;
    }
    .footer-text {
      font-size: 14px;
      color: #6b7280;
      margin: 4px 0;
    }
    
    /* Mobile Responsive */
    @media only screen and (max-width: 600px) {
      .email-wrapper {
        margin: 20px;
        border-radius: 16px;
      }
      .content {
        padding: 32px 24px;
      }
      .header {
        padding: 32px 24px;
      }
      .tracking-hero, .package-prep, .details-grid {
        padding: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <!-- Modern Header -->
    <div class="header">
      <div class="logo">🚗 AutoXpress</div>
      <div class="header-subtitle">Your Order Has Shipped</div>
    </div>
    
    <!-- Content -->
    <div class="content">
      <div class="greeting">Great news, <%= @customer.name %>! 🎉</div>
      <div class="intro-text">
        Your <strong><%= @order.product_name %></strong> for your <%= @order.car_year %> <%= @order.car_make_model %> 
        has been carefully packaged and is now on its way to you.
      </div>
      
      <!-- Tracking Hero or Package Prep -->
      <% if @dispatch&.tracking_number.present? %>
        <div class="tracking-hero">
          <h3>📍 Track Your Package</h3>
          <div class="tracking-number"><%= @dispatch.tracking_number %></div>
          <% if @dispatch.tracking_link.present? %>
            <a href="<%= @dispatch.tracking_link %>" class="track-button">Track Your Package</a>
          <% end %>
        </div>
      <% else %>
        <div class="package-prep">
          <h3>📦 Package Preparation</h3>
          <p>Your order has been processed and is being prepared for shipment.</p>
          <p><strong>We'll send you tracking details within 24 hours!</strong></p>
        </div>
      <% end %>
      
      <!-- Order Details -->
      <div class="details-grid">
        <h4>📋 Order Details</h4>
        <div class="detail-item">
          <span class="detail-label">Order Number:  </span>
          <span class="detail-value">#<%= @order.order_number %></span>
        </div>
        <div class="detail-item">
          <span class="detail-label">Product:  </span>
          <span class="detail-value"><%= @order.product_name %></span>
        </div>
        <div class="detail-item">
          <span class="detail-label">Vehicle:  </span>
          <span class="detail-value"><%= @order.car_year %> <%= @order.car_make_model %></span>
        </div>
        <% if @dispatch %>
          <div class="detail-item">
            <span class="detail-label">Status:  </span>
            <span class="detail-value"><span class="status-badge">In Transit</span></span>
          </div>
          <div class="detail-item">
            <span class="detail-label">Estimated Delivery:  </span>
            <span class="detail-value"><%= @order.estimated_delivery&.strftime("%B %d, %Y") || "2-3 business days" %></span>
          </div>
        <% end %>
        <div class="detail-item">
          <span class="detail-label">Shipping Address:  </span>
          <span class="detail-value"><%= @order.customer_address %></span>
        </div>
      </div>
      
      <!-- What to Expect -->
      <div class="expectations">
        <h4>🚚 What to Expect</h4>
        <ul>
          <li><strong>Tracking Updates: </strong> Monitor your package's journey in real-time</li>
          <li><strong>Delivery Window: </strong> Most packages arrive within 2-3 business days</li>
          <li><strong>Installation Support: </strong> Expert guidance available at (555) 123-AUTO</li>
          <li><strong>Quality Guarantee: </strong> 100% satisfaction or your money back</li>
        </ul>
      </div>
      
      <div style="background: #f0f9ff; padding: 24px; border-radius: 12px; text-align: center; margin: 32px 0;">
        <p style="margin: 0; font-size: 16px; color: #1e40af;">
          <strong>Questions about your shipment?</strong><br>
          Reply to this email or call us at <strong>(555) 123-AUTO</strong>
        </p>
      </div>
    </div>
    
    <!-- Footer -->
    <div class="footer">
      <div class="footer-brand">AutoXpress</div>
      <div class="footer-text">Premium Automotive Parts & Service</div>
      <div class="footer-text">Order #<%= @order.order_number %> • <%= Date.current.year %> AutoXpress</div>
    </div>
  </div>
</body>
</html>